name: Temp Action

on:
  workflow_dispatch:

jobs:
  runs-on: self-hosted
  no-u:
      - name: Clear existing images cache
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh cache delete astro-images-dev --confirm