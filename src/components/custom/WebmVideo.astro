---
const { src, width = 640, height = 360, autoplay = false, loop = false, muted = false, style } =
  Astro.props;
---
<video
  width={width}
  height={height}
  {...(autoplay ? { autoplay: true } : {})}
  {...(loop ? { loop: true } : {})}
  {...(muted ? { muted: true } : {})}
  class={style}
>
  <source src={src} type="video/webm">
  Your browser does not support webm.
</video>